const YES = 'yes'
const NO = 'no'

const info = function (data) {
    console.info(data)
}

const table = function (data) {
    console.table(data)
}

const page = {
    customer: '#p-customer',
    index: '#p-index',
    order: '#p-order',
    product: '#p-product',
}

const modal = {
    confirm: '#modal-confirm',
}

